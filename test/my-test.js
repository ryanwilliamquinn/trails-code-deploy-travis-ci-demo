'use strict'

const assert = require('assert')

describe('stand-in tests to explore how code deploy and travis ci work together', () => {
  it('should do test stuff', () => {
    assert(true)
  })
})
